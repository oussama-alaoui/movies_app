import { ColorValue } from 'react-native';
import { SvgProps } from 'react-native-svg';
declare const Volume3: {
    (props: Props): JSX.Element;
    defaultProps: {
        color: string;
        size: string;
    };
};
export default Volume3;
interface Props extends SvgProps {
    color?: ColorValue;
    size?: string | number;
}
