import { GestureResponderEvent } from 'react-native';
import { Dimension } from './NexenPlayer';
declare type PlayButtonProps = {
    dimension?: Dimension;
    onPlayPress?: (event: GestureResponderEvent) => void;
};
declare const PlayButton: {
    (props: PlayButtonProps): JSX.Element;
    defaultProps: {
        dimension: {
            width: number;
            height: number;
        };
    };
};
export default PlayButton;
